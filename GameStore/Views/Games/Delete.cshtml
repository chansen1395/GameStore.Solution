@{
  Layout = "_Layout";
}

@model GameStore.Models.Game

<h2>Are you sure you want to delete this?</h2>

@Html.DisplayNameFor(model => model.GameName): @Html.DisplayFor(model => model.GameName)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}

<div id="footer">
  <button class="bottom">@Html.ActionLink("Back to List", "Index")</button>
  <button class="bottom">@Html.ActionLink("Delete Game", "Delete", new { id = Model.GameId })</button>
</div>